"use client";
import React from "react";
import CourseTemplate from "@/components/CourseTemplate";
// TODO: Add Electronics curriculum data and import when available

const Electronics = () => {
  return <CourseTemplate courseId="electronics" curriculumData={[]} />;
};

export default Electronics;
